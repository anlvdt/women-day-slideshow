/**
 * Admin configuration — Copy this file to admin-config.js and set your values.
 * admin-config.js is gitignored to keep credentials out of version control.
 */
export const ADMIN_PASSWORD = "your-admin-password-here";
